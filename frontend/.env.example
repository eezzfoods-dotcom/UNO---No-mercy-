# Only needed when the client and server run on SEPARATE hosts.
# Left unset, the client talks to the same origin it was served from —
# which is what a single deployed service wants.
#
# For local split dev (Vite on 5174, server on 3002):
# VITE_UNO_BACKEND_URL=http://localhost:3002
